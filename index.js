// Root entry point — see /server/index.js for the Express backend
// and /client/ for the React frontend.
//
// Quick start:
//   cd server && npm install && node index.js
//   cd client && npm install && npm run dev
console.log('Vaqt — see README.md for run instructions');
